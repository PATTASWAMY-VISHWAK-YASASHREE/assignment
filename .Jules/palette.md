## 2024-03-24 - Drag and Drop File Upload
**Learning:** MUI `Box` with `border: 'dashed'` and conditional `bgcolor` using `alpha(primary.main, 0.05)` creates an effective and consistent drop zone.
**Action:** Use this pattern for future file upload areas or draggable components.
